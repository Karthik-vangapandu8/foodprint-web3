<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/head') %>
    <link rel="stylesheet" href="/css/voice-input.css">
    <title>फसल जोड़ें | Add Harvest - FoodPrint</title>
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }
        .simple-container {
            max-width: 700px;
            margin: 50px auto;
            padding: 40px;
            background: white;
            border-radius: 20px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.2);
        }
        .big-icon {
            font-size: 80px;
            text-align: center;
            margin-bottom: 20px;
        }
        .bilingual-title {
            text-align: center;
            margin-bottom: 40px;
        }
        .bilingual-title h1 {
            font-size: 32px;
            color: #333;
            margin-bottom: 10px;
        }
        .bilingual-title p {
            font-size: 18px;
            color: #666;
        }
        .big-button {
            width: 100%;
            padding: 25px;
            font-size: 24px;
            font-weight: 700;
            border: none;
            border-radius: 15px;
            margin-bottom: 15px;
            cursor: pointer;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 15px;
        }
        .big-button-voice {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }
        .big-button-voice:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(102,126,234,0.4);
        }
        .crop-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
            margin: 30px 0;
        }
        .crop-card {
            padding: 30px;
            text-align: center;
            background: #f8f9fa;
            border: 4px solid transparent;
            border-radius: 15px;
            cursor: pointer;
            transition: all 0.3s;
        }
        .crop-card:hover {
            border-color: #667eea;
            transform: scale(1.05);
        }
        .crop-card.selected {
            border-color: #667eea;
            background: #e8eaf6;
        }
        .crop-card-icon {
            font-size: 60px;
            margin-bottom: 10px;
        }
        .crop-card-name {
            font-size: 18px;
            font-weight: 700;
            color: #333;
            margin-bottom: 5px;
        }
        .crop-card-name-en {
            font-size: 14px;
            color: #666;
        }
        .input-large {
            width: 100%;
            padding: 20px;
            font-size: 20px;
            border: 3px solid #e0e0e0;
            border-radius: 10px;
            margin-bottom: 20px;
            transition: all 0.3s;
        }
        .input-large:focus {
            border-color: #667eea;
            outline: none;
        }
        .submit-btn {
            background: #4CAF50;
            color: white;
            width: 100%;
            padding: 25px;
            font-size: 24px;
            font-weight: 700;
            border: none;
            border-radius: 15px;
            cursor: pointer;
            transition: all 0.3s;
        }
        .submit-btn:hover {
            background: #45a049;
            transform: translateY(-3px);
        }
        .label-large {
            font-size: 22px;
            font-weight: 700;
            color: #333;
            margin-bottom: 10px;
            display: block;
        }
        .label-small {
            font-size: 16px;
            color: #666;
            margin-bottom: 15px;
            display: block;
        }
    </style>
</head>
<body>
    <div class="simple-container">
        <div class="big-icon">🌾</div>
        <div class="bilingual-title">
            <h1>फसल जोड़ें</h1>
            <p>Add Your Harvest</p>
        </div>

        <button type="button" class="big-button big-button-voice" onclick="speakInstructions()">
            🎤 बोलें | Speak to Fill Form
        </button>

        <form action="/app/harvest/save" method="POST" id="simpleHarvestForm">
            <!-- Crop Selection -->
            <label class="label-large">1️⃣ फसल चुनें | Select Crop:</label>
            <div class="crop-grid">
                <div class="crop-card" onclick="selectCrop('Tomato', '🍅', 'टमाटर', this)">
                    <div class="crop-card-icon">🍅</div>
                    <div class="crop-card-name">टमाटर</div>
                    <div class="crop-card-name-en">Tomato</div>
                </div>
                <div class="crop-card" onclick="selectCrop('Potato', '🥔', 'आलू', this)">
                    <div class="crop-card-icon">🥔</div>
                    <div class="crop-card-name">आलू</div>
                    <div class="crop-card-name-en">Potato</div>
                </div>
                <div class="crop-card" onclick="selectCrop('Wheat', '🌾', 'गेहूं', this)">
                    <div class="crop-card-icon">🌾</div>
                    <div class="crop-card-name">गेहूं</div>
                    <div class="crop-card-name-en">Wheat</div>
                </div>
                <div class="crop-card" onclick="selectCrop('Onion', '🧅', 'प्याज', this)">
                    <div class="crop-card-icon">🧅</div>
                    <div class="crop-card-name">प्याज</div>
                    <div class="crop-card-name-en">Onion</div>
                </div>
                <div class="crop-card" onclick="selectCrop('Carrot', '🥕', 'गाजर', this)">
                    <div class="crop-card-icon">🥕</div>
                    <div class="crop-card-name">गाजर</div>
                    <div class="crop-card-name-en">Carrot</div>
                </div>
                <div class="crop-card" onclick="selectCrop('Cabbage', '🥬', 'पत्तागोभी', this)">
                    <div class="crop-card-icon">🥬</div>
                    <div class="crop-card-name">पत्तागोभी</div>
                    <div class="crop-card-name-en">Cabbage</div>
                </div>
            </div>

            <!-- Hidden fields for backend -->
            <input type="hidden" name="viewmodal_harvest_suppliershortcode" value="FARM" required>
            <input type="hidden" name="viewmodal_harvest_suppliername" id="farmName" value="My Farm" required>
            <input type="hidden" name="viewmodal_harvest_supplieraddress" value="Farm Location">
            <input type="hidden" name="viewmodal_harvest_producename" id="cropInput" required>
            <input type="hidden" name="viewmodal_supplierproduce" id="cropCode" required>
            <input type="hidden" name="viewmodal_harvest_timestamp" id="harvestDate">
            <input type="hidden" name="viewmodal_harvest_geolocation" id="geolocation" value="0,0">

            <!-- Quantity -->
            <label class="label-large">2️⃣ मात्रा | Quantity:</label>
            <label class="label-small">कितनी फसल काटी? | How much did you harvest?</label>
            <input type="number" name="viewmodal_harvest_quantity" class="input-large" placeholder="जैसे: 100" required min="1">

            <!-- Unit -->
            <label class="label-large">3️⃣ इकाई | Unit:</label>
            <select name="viewmodal_harvest_unitofmeasure" class="input-large" required>
                <option value="">इकाई चुनें | Select Unit</option>
                <option value="kg">किलोग्राम | Kilogram (kg)</option>
                <option value="quintal">क्विंटल | Quintal</option>
                <option value="ton">टन | Ton</option>
            </select>

            <!-- Description -->
            <label class="label-large">4️⃣ विवरण | Description:</label>
            <textarea name="viewmodal_harvest_description" class="input-large" rows="3" placeholder="कुछ बताएं... | Tell us something..." required style="resize: vertical;"></textarea>

            <!-- Submit Button -->
            <button type="submit" class="submit-btn">
                ✅ सहेजें | Save Harvest
            </button>
        </form>

        <!-- Back Button -->
        <a href="/app/harvest" style="display: block; text-align: center; margin-top: 20px; color: #667eea; font-size: 18px; text-decoration: none;">
            ← वापस जाएं | Go Back
        </a>
    </div>

    <%- include('partials/scripts') %>
    <script src="/js/voice-input.js"></script>

    <script>
        let selectedCrop = null;

        // Auto-set current date
        document.getElementById('harvestDate').value = new Date().toISOString().split('T')[0];

        // Get user location
        if (navigator.geolocation) {
            navigator.geolocation.getCurrentPosition(function(position) {
                const lat = position.coords.latitude.toFixed(6);
                const lon = position.coords.longitude.toFixed(6);
                document.getElementById('geolocation').value = lat + ',' + lon;
                console.log('📍 Location set:', lat, lon);
            });
        }

        function selectCrop(cropName, icon, hindiName, element) {
            // Remove previous selection
            document.querySelectorAll('.crop-card').forEach(card => card.classList.remove('selected'));
            
            // Select this card
            element.classList.add('selected');
            selectedCrop = cropName;
            
            // Fill hidden fields
            document.getElementById('cropInput').value = cropName;
            document.getElementById('cropCode').value = cropName + '_CODE';
            document.getElementById('farmName').value = '<%= user.firstName %> <%= user.lastName %> Farm';
            
            // Speak confirmation
            if (window.VoiceInputManager) {
                window.VoiceInputManager.speakText(`आपने ${hindiName} चुना। ${cropName} selected.`);
            }
        }

        function speakInstructions() {
            if (!window.VoiceInputManager || !window.VoiceInputManager.isSupported) {
                alert('वॉइस सपोर्ट नहीं है। कृपया Chrome या Edge ब्राउज़र का उपयोग करें।');
                return;
            }

            window.VoiceInputManager.speakText(
                'पहले फसल चुनें। फिर मात्रा बोलें। फिर इकाई चुनें। अंत में विवरण बोलें।'
            );

            // Enable voice for quantity and description
            setTimeout(() => {
                const quantityInput = document.querySelector('input[name="viewmodal_harvest_quantity"]');
                const descriptionInput = document.querySelector('textarea[name="viewmodal_harvest_description"]');
                
                if (quantityInput && descriptionInput) {
                    alert('🎤 अब आप किसी भी फील्ड पर क्लिक करके बोल सकते हैं।');
                }
            }, 3000);
        }

        // Form validation
        document.getElementById('simpleHarvestForm').addEventListener('submit', function(e) {
            if (!selectedCrop) {
                e.preventDefault();
                alert('⚠️ कृपया पहले फसल चुनें! | Please select a crop first!');
                window.VoiceInputManager.speakText('कृपया पहले फसल चुनें');
                return false;
            }
        });

        // Welcome message
        setTimeout(() => {
            if (window.VoiceInputManager && window.VoiceInputManager.isSupported) {
                window.VoiceInputManager.speakText('नमस्ते <%= user.firstName %>। अपनी फसल का विवरण भरें।');
            }
        }, 1000);
    </script>
</body>
</html>
